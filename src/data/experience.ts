import type { ProfileTimelineEntry } from './profile-timeline'

/**
 * Experience stays data-driven even while there are no confirmed entries to
 * publish. Add records here when an experience is ready to be shown.
 */
export const experienceEntries: readonly ProfileTimelineEntry[] = []
